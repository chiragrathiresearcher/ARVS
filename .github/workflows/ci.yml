name: ARVS CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:

  # ============================================================
  # Job 1: Python — Lint + Full Test Suite (69 deterministic tests)
  # ============================================================
  python-tests:
    name: Python Tests
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install numpy pandas matplotlib psutil pytest flake8

      - name: Lint (flake8)
        run: |
          flake8 ARVS/ simulation/ \
            --max-line-length=120 \
            --ignore=E203,W503,E501 \
            --exclude=simulation/outputs

      - name: Run test suite
        working-directory: ${{ github.workspace }}
        run: |
          pytest simulation/tests/test_arvs_full.py -v \
            --tb=short \
            --no-header \
            -q

  # ============================================================
  # Job 2: C++ Core — Standalone build (portable, no ROS2 needed)
  # Compiles arvs_core library: SafetyGate, AxiomValidator,
  # WatchdogTimer, ExecutionController
  # ============================================================
  cpp-core-build:
    name: C++ Core Build
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y cmake ninja-build g++-12

      - name: Configure (standalone — no ROS2)
        working-directory: cpp
        run: |
          cmake -B build \
            -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER=g++-12 \
            -DBUILD_TESTING=OFF \
            -DCMAKE_CXX_FLAGS="-Wall -Wextra -fstack-protector-strong" \
            -DARVS_STANDALONE=ON
        # ARVS_STANDALONE=ON skips ament/ROS2 node targets
        # arvs_core static library builds without any ROS2 dependency

      - name: Build arvs_core
        working-directory: cpp
        run: cmake --build build --parallel

      - name: Upload arvs_core artifact
        uses: actions/upload-artifact@v4
        with:
          name: arvs-core-lib
          path: cpp/build/libarvs_core.a

  # ============================================================
  # Job 3: Simulation — All 16 scenarios across 4 telemetry sources
  # ============================================================
  simulation:
    name: Simulation (16 scenarios)
    runs-on: ubuntu-22.04
    needs: python-tests

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install numpy pandas matplotlib psutil

      - name: Run simulation
        working-directory: ${{ github.workspace }}
        run: python simulation/run_simulation.py

      - name: Upload simulation reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: simulation-reports
          path: |
            simulation/outputs/reports/
            simulation/outputs/forensics/
